other:
  name: Ostatní
  view: yes
  entities:
    - device_tracker.marcelka_telefon
    - device_tracker.pavel_telefon
    - device_tracker.my_sensors
    - group.system
    - group.batteries

batteries:
  name: Baterie
  entities:
    - sensor.palm_battery_template
    - sensor.living_temp_batteries
    - sensor.bedroom_temp_batteries
    - sensor.kitchen_temp_batteries
    - sensor.outside_temp_batteries

system:
  name: Systém
  entities:
    - sensor.since_last_boot_templated
    - sensor.ram_use
    - sensor.cpu_use
    - sensor.disk_use_
    - sensor.speedtest_ping
    - sensor.speedtest_download
    - sensor.speedtest_upload